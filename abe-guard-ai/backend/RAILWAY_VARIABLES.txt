# Set these in Railway → Guard AI service (root: abe-guard-ai/backend) → Variables.
# JWT_SECRET and DATABASE_URL must match the Admin backend service.

NODE_ENV=production
DATABASE_URL=${{ Postgres.DATABASE_URL }}
JWT_SECRET=your-secret-at-least-16-characters-long
CORS_ORIGINS=https://admin-dashboard-frontend-flax.vercel.app,https://admin-dashboard-frontend-techworldstarzllcs-projects.vercel.app,https://frontend-guard-ui.vercel.app

# AI ranking (optional — without this, callouts use rule-based ranking)
# OPENAI_API_KEY=sk-...

# SMS (optional on Guard AI — admin backend can also SMS if Twilio is set there)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_MESSAGING_SERVICE_SID=   # preferred
# TWILIO_PHONE_NUMBER=            # or From number

# Email from Guard AI (optional — admin SMTP also emails ranked guards after proxy)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=

# Voice callouts (optional — also set on ADMIN for unified outbound)
# CALLOUT_ENABLE_VOICE_CALL=true
# TWILIO_PHONE_NUMBER=+18557942422
# PUBLIC_BASE_URL=https://admin-dashboard-production-2596.up.railway.app

# Realtime (optional — in-app callout alerts + admin live updates)
# REDIS_URL=${{ Redis.REDIS_URL }}

# Then on ADMIN backend service, set:
# ABE_GUARD_AI_URL=https://THIS-SERVICE-PUBLIC-URL.up.railway.app
# Plus SMTP_* (email) and TWILIO_* (SMS) so ranked guards like Ghazi get notified outside the app.
#
# === ADMIN service (unified outbound after AI ranking) — required for live SMS/email/calls ===
# TWILIO_ACCOUNT_SID=...
# TWILIO_AUTH_TOKEN=...
# TWILIO_MESSAGING_SERVICE_SID=MG...   # SMS
# TWILIO_PHONE_NUMBER=+18557942422     # required for voice From
# PUBLIC_BASE_URL=https://admin-dashboard-production-2596.up.railway.app
# CALLOUT_OUTBOUND_EMAIL=true
# CALLOUT_OUTBOUND_SMS=true
# CALLOUT_OUTBOUND_CALL=true           # opt-in; Twilio voice charges apply
# SMTP_HOST / SMTP_USER / SMTP_PASS / SMTP_FROM
