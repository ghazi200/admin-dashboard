{
  "name": "admin-dashboard-monorepo",
  "private": true,
  "description": "Root for Railway; Admin backend runs from backend/",
  "scripts": {
    "start": "cd backend && node server.js",
    "install": "cd backend && npm install",
    "migrate:railway": "node run-railway-migrations.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "sequelize": "^6.35.0"
  }
}
