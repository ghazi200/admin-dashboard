# Optional: point API requests directly at the backend (bypasses CRA proxy).
# REACT_APP_API_URL=http://localhost:5000

# Realtime: WebSocket Gateway URL (production). Set in Vercel for deployed app.
# REACT_APP_WS_GATEWAY_URL=https://your-websocket-gateway.up.railway.app

# Geographic Dashboard (Map): Google Maps JavaScript API key.
# REACT_APP_GOOGLE_MAPS_API_KEY=your_key_here
